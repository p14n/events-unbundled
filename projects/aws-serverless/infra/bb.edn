{:paths ["clj"]
 :min-bb-version "0.4.0"

 :tasks
 {:requires ([infra :as inf]
             [cheshire.core :as json])


  synth {:doc "Creates terraform"
         :task (->> (inf/synth) (json/generate-string) (spit "transient.tf.json"))}}}
